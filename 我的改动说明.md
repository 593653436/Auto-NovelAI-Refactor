# 我的改动说明（相对官方 v2.0.3）

> 本 fork（`593653436/Auto-NovelAI-Refactor`）在官方 `zhulinyv/Auto-NovelAI-Refactor` 基础上新增了 2 个功能。
> 改动文件：`web/js/views/generate.js`、`web/js/components.js`、`utils/services/pnginfo.py`。

---

## 功能 1：生成页拖入图片 → 自动导入提示词 + 角色分区

**一句话**：在「🖼️ 图片生成」主页面，把 NovelAI 导出的图片**拖进网页任意空白处**，松开即自动填好正向/负向提示词 + 角色分区（每个角色：正面/负面提示词 + 位置 + 启用）。**不加任何按钮/入口**。

**改动**：
- `web/js/views/generate.js`：新增 `attachGlobalDrop()`（全局拖放监听 + 全屏遮罩反馈 + `uploadFiles → /api/pnginfo/to-generate → setGenerateState` 自动导入）；新增 `applyImportedState()`（导入时按当前模型适配角色位置：V5 用自由坐标 `x,y`，其余用 A1-E5 网格，保证位置精度）。
- `web/js/views/generate.js`：顶部 import 增加 `uploadFiles`。
- `utils/services/pnginfo.py`：`pnginfo_to_generate()` 的每个角色记录**额外带原始连续坐标 `x`/`y`（0-1）**，供前端 V5 自由坐标模式精度恢复（否则 "A1" 网格串在 V5 下会丢位置）。

**行为细节**：
- 拖入时避开子组件自带拖放区（参考图 / 风格迁移 / 图生图编辑器 / 输入框），这些区域仍优先交给子组件处理。
- 图片若被抹除元数据/已转格式（读不到生成参数）：提示「未读到生成参数，可到🔮法术解析→图片反推」，**不误清空当前表单**。

---

## 功能 2：可视化角色位置（V5 自由画布 + V4.5 5×5 网格）

复刻 NovelAI 官网的角色位置交互（官网 V5 = 自由拖动圆点、无网格；V4.5 = 5×5 网格点选）。

### 2-A V4.5：5×5 网格点选（不止下拉）
**改动**：`web/js/components.js` 的 `position` 控件在 grid 模式下，下拉框旁新增 **「🗖 网格」按钮** → 点开 5×5 格子 → 点选某格即设置该角色位置（A1-E5），对齐官网 V4.x 交互。

### 2-B V5：共享自由画布拖拽
**改动**：`web/js/views/generate.js` 新增 `buildPositionCanvas()`，在「👥 角色分区」面板顶部挂一个共享画布：

- **V5 模型**下显示；画布上每个角色一个**编号圆点**（对应角色列表序号），**鼠标拖动圆点**自由设置其在画面中的位置；
- 顶部「🕸 网格线」开关（默认关）——开启后画布显示 5×5 辅助网格线（对应官网 V5 的可选网格）；
- 「✅ 完成位置编辑」→ 把各角色当前位置写回角色分区 → 生成时后端 `_build_character_data()` 把它转成 `centers[{x,y}]` 发给 NovelAI（后端零改动）；
- 画布长宽比**实时跟随**当前设置的分辨率（宽/高）——分辨率预设 / 宽 / 高任一变化，画布立即缩放到对应比例，使圆点位置即图内真实位置。

**范围**：仅 V5 用自由画布；V4.5 维持 5×5 网格（与官网一致）。AI's Choice 模式下画布圆点仍可拖，但以 Custom 模式为准（与官网逻辑一致）。

---

## 验证情况
- ✅ `utils/services/pnginfo.py` py_compile 通过
- ✅ `generate.js` / `components.js` node `--check` 语法通过
- ✅ 浏览器实际加载（共享浏览器 9223）无 JS 报错
- ✅ V4.5 角色分区：`🗖 网格` 按钮 + 25 格 5×5 面板渲染正常
- ✅ 切到 V5 模型：「🎨 角色位置」画布显示 + 编号圆点 + 网格线开关
- ✅ 画布长宽比随分辨率实时变化（实测 832×1216 ↔ 1216×832 切换）

> 拖放导入、画布拖拽手感、真实生成效果需人工实测（用带角色的 NovelAI PNG 拖入；V5 下拖圆点生成）。

---

## 角色分区交互增强（components.js，通用）
- **拖拽排序**：每张角色卡片左上 `⠿` 手柄，拖动可调整角色顺序（角色分区 / 角色参考均生效）。
- **单独删除**：每张卡片右上 `✖` 删除该角色（不再只能删最后一个）。
- **非启用置灰**：取消勾选"启用"的卡片灰显（透明度 + 灰度），一眼可辨。

---

## 工位模式（防偷看/摸鱼掩护，前后端）
- 设置页新增「🕶️ 工位模式」开关，开启后**所有内容图片高斯模糊**，**点击图片可查看原图**（再点恢复模糊）。
- UI 图标（emoji/svg/logo）不模糊，仅内容位图模糊；保存即时生效、无需重启。
- 改动：`utils/config.py`(wework_mode)+`web/js/ui.js`(applyPeekMode)+`web/js/app.js`(启动应用)+`web/js/views/settings.js`(开关项)。

---

## 0.3 ComfyUI 反推接入（图片反推页）
- 反推页新增 **0.3 ComfyUI 在线灯**（绿=在线 / 灰=离线）与**反推引擎下拉**：`WD (HF Space)` / `PixAI v0.9 (0.3)` / `Qwen3-VL (0.3)`。
- 0.3 在线时启用 PixAI / Qwen 两个新引擎；离线时自动只留 WD。
- PixAI Tagger v0.9：0.3 ComfyUI 节点 `PixAITagger|adbrasi`（ONNX `deepghs/pixai-tagger-v0.9-onnx`），输出 Danbooru tags（识别**更新角色**）。
- Qwen3-VL：0.3 ComfyUI 节点 `AILab_QwenVL_GGUF`（本地 GGUF `Qwen3VL-8B-Instruct-Q4_K_M.gguf`，不联网），输出**自然语言描述**。
- 走 ComfyUI API：上传图 → 提交 workflow → 轮询 history（结果取 PreviewAny.text）。
- 新增 `utils/services/comfyui_tagger.py`；`/api/tagger` 支持 `engine=wd|pixai|qwen`；`/api/tagger/status` 返回在线状态。

---

## 💬 Qwen3-VL 对话（反推页新 tab）
- 反推页新增「💬 Qwen 对话」tab：上传图 + 输入任意问题 → Qwen3-VL 按提问回答（提取人物 / 风格 / 动作 / 服装等）。
- 走 `/api/tagger/qwen-chat`（图 + 提问，qwen_vl 的 `custom_prompt`），支持自定义提示词。
- 例：`extract this character name, hairstyle, outfit and overall style` → 返回结构化提取。

---

## 说明
- 后端零改动即可生成（角色位置 → `centers` 序列化早已具备）。
- 官方 `update.bat`/`git pull` 会覆盖本改动；本 fork 通过 `origin`(自己) + `upstream`(官方) 管理，官方更新需 `git fetch upstream && git merge upstream/main` 手动合并。
